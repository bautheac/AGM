pacman::p_load(modules)


modules::export("event_date_variables")
event_date_variables <- c(
  "date of signing the letter", "date of auditing", "first day of closed books", 
  "last day of closed books", "date of AGM"
)